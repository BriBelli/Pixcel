---
name: pixcel-vision
description: >-
  Pixcel's living strategy memory — the max-potential thesis and the use-case map (immediate →
  far-future, obvious → extreme niche). Apply when reasoning about Pixcel's DIRECTION: potential,
  roadmap, prioritization, markets, monetization, moat, or "what is this really / what's it for."
  NOT for generating art (use the `pixcel` skill) — this is the why and where, not the how. A LIVING
  doc: update it as milestones land and the vision sharpens (the "bigger statue" gets shaped).
---

# Pixcel Vision (living strategy)

Two maintained docs, kept honest and current:

- **[MAX-POTENTIAL.md](MAX-POTENTIAL.md)** — what this is at its ceiling, *unbiased*: the product layer
  AND the deeper Statue-Method-as-a-pattern layer, the real moat, and the honest constraints.
- **[USE-CASES.md](USE-CASES.md)** — what it's for, immediate → long-term, obvious → extreme niche,
  plus the cross-domain "bigger statue" expansions.
- **[PLATFORM-VISION.md](PLATFORM-VISION.md)** — the merge thesis: weaving the multi-LLM + image/video
  project together with the Statue Method, on the shared media-as-JSON primitive. Pixel art = vertical #1.

## How to use
When the conversation is about *strategy/direction* (not building a feature, not making art), read the
relevant doc, reason from it, and **propose updates** when reality moves it. Tie claims to what's
*proven* vs *hypothesized* — that honesty is the point.

## The frame (Brian's "bigger statue")
We are sculpting statues at two scales at once:
- **The small statue** — a single piece of pixel art (VISION→SHAPE→POLISH→QA+keep-best). **Proven (M2).**
- **The big statue** — Pixcel itself (the product, the method, the company). We are at its **SHAPE/
  early-refine** stage. Same method applies: commit the vision, rough the form, lock what's good,
  refine, never regress.

## Maintenance rule
This skill is **expected to drift toward truth.** Each milestone, revisit: did a "hypothesis" become
"proven"? Did a use-case become real, or die? Did the ceiling move? Edit the docs; keep the
proven/hypothesized line sharp. Stale strategy is worse than none.
